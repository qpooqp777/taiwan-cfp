---
name: taiwan-cfp
description: 台灣碳足跡排放係數資料集查詢與分析工具。查詢環保署公開資料集中的碳足跡排放係數、進行排放量計算、生成碳足跡報告。Keywords: 碳足跡, 排放係數, CFP, 環保署, 碳盤查, carbon footprint.
---

# 台灣碳足跡排放係數 (Taiwan CFP)

台灣環保署公開資料集查詢與分析工具。快速查詢產品碳足跡排放係數、計算排放量、生成碳盤查報告。

## 功能

- **查詢排放係數**：按產品類別、行業、物質名稱搜尋排放係數
- **計算排放量**：基於排放係數計算產品/活動的碳足跡
- **數據分析**：統計、比較、趨勢分析
- **報告生成**：自動生成碳盤查報告（支援 Excel、PDF）
- **數據緩存**：本地緩存減少 API 調用

## 快速開始

### 1. 查詢排放係數

```
查詢電力的排放係數
查詢水泥產業的碳足跡係數
搜尋「天然氣」的排放係數
```

### 2. 計算排放量

```
計算 1000 度電的碳足跡
計算生產 500 公斤水泥的排放量
```

### 3. 生成報告

```
生成我的碳盤查報告
匯出排放係數清單為 Excel
```

## 工作流程

### 基本流程

1. **初始化連接**：使用 `scripts/cfp_client.py` 連接環保署 API
2. **查詢數據**：根據用戶需求搜尋排放係數
3. **數據處理**：解析 JSON 響應，提取關鍵信息
4. **計算分析**：執行排放量計算或統計分析
5. **結果呈現**：以表格、圖表或報告形式展示

### 進階工作流程

- **批量查詢**：支援多個產品/活動的批量計算
- **趨勢分析**：對比不同時期的排放係數變化
- **報告生成**：自動生成可視化碳盤查報告
- **數據導出**：支援 CSV、Excel、JSON 格式

## API 說明

### 環保署開放資料集

**資料集名稱**：台灣碳足跡排放係數  
**API 端點**：`https://data.moenv.gov.tw/api/v2/CFP_P_02`

**必需參數**：
- `api_key`：環保署 API 金鑰（需自行申請）
- `limit`：返回筆數（預設 1000）
- `sort`：排序方式（預設 `ImportDate desc`）
- `format`：回應格式（`json` 或 `xml`）

**可選參數**：
- `filter`：篩選條件（如產品類別、行業別）
- `offset`：分頁偏移

### 回應格式

```json
{
  "success": true,
  "info": {
    "count": 1000,
    "limit": 1000,
    "offset": 0
  },
  "result": [
    {
      "id": "CFP_001",
      "ProductName": "電力",
      "EmissionFactor": 0.509,
      "Unit": "kg CO2e/kWh",
      "DataYear": 2024,
      "Source": "台灣電力公司",
      "ImportDate": "2024-01-15"
    }
  ]
}
```

## 使用示例

### 示例 1：查詢電力排放係數

**用戶請求**：「查詢 2024 年電力的碳足跡係數」

**工作流程**：
1. 調用 `scripts/cfp_client.py query --product "電力" --year 2024`
2. 解析 API 響應
3. 提取排放係數、單位、數據來源
4. 以表格形式展示結果

**預期輸出**：
```
產品名稱: 電力
排放係數: 0.509 kg CO2e/kWh
數據年份: 2024
資料來源: 台灣電力公司
更新日期: 2024-01-15
```

### 示例 2：計算排放量

**用戶請求**：「計算使用 5000 度電的碳足跡」

**工作流程**：
1. 查詢電力排放係數（0.509 kg CO2e/kWh）
2. 計算：5000 kWh × 0.509 = 2545 kg CO2e
3. 轉換單位：2.545 公噸 CO2e
4. 提供對標信息（如相當於種植多少棵樹）

**預期輸出**：
```
電力使用量: 5000 kWh
排放係數: 0.509 kg CO2e/kWh
總排放量: 2545 kg CO2e (2.545 公噸)
相當於: 種植 127 棵樹一年的吸收量
```

### 示例 3：生成碳盤查報告

**用戶請求**：「生成我的碳盤查報告」

**工作流程**：
1. 收集用戶的排放活動清單
2. 逐項查詢排放係數
3. 計算各項排放量
4. 統計總排放量
5. 生成可視化報告（含圖表、對標、建議）
6. 導出為 Excel 或 PDF

## 資源

### 腳本

- `scripts/cfp_client.py`：Python 客戶端，封裝 API 調用、數據解析、計算邏輯
- `scripts/cfp_batch.py`：批量查詢與計算工具
- `scripts/cfp_report.py`：報告生成工具

### 配置

- `config/api_config.json`：API 金鑰、端點配置
- `config/categories.json`：產品類別映射表
- `config/units.json`：單位轉換表

### 數據

- `data/cfp_cache.json`：本地排放係數緩存
- `data/emission_factors.csv`：排放係數參考表

## 常見問題

**Q: 如何獲取 API 金鑰？**  
A: 訪問 [環保署開放資料平台](https://data.moenv.gov.tw/)，註冊帳號後申請 API 金鑰。

**Q: 排放係數多久更新一次？**  
A: 通常每年更新一次，具體日期見 `ImportDate` 欄位。

**Q: 支援哪些產品類別？**  
A: 支援電力、天然氣、水、水泥、鋼鐵、紙張等主要產業，詳見 `config/categories.json`。

**Q: 可以離線使用嗎？**  
A: 可以。首次查詢後會緩存數據到 `data/cfp_cache.json`，後續可離線查詢。

## 相關資源

- [環保署開放資料平台](https://data.moenv.gov.tw/)
- [台灣碳足跡標籤](https://cfp.moenv.gov.tw/)
- [ISO 14067 碳足跡標準](https://www.iso.org/standard/71096.html)
